---
name: sdd-module-design-gate
description: 对当前 SDD 工作流指定的模块详细设计类成果物进行质量门禁检查，围绕证据链、边界链、执行链和风险链判断 ASIS、TOBE、AICoding 任务和风险设计是否足以支撑后续编码。当前需要确认模块详设是否可进入 AICoding、发现阻断问题、生成整改清单、驱动回到 ASIS 或 TOBE 补齐并多轮复检时使用。若工作流未指定目标成果物，默认检查“xxx模块详细设计说明书”。
---

# SDD 模块设计门禁

## 方法论

先阅读共享方法论：`C:\Users\15802\.codex\skills\sdd-module-design-method.md`。

门禁不检查“章节是否写满”，而检查四条链是否闭合：

- 证据链：需求/AR -> ASIS 证据 -> TOBE 决策。
- 边界链：模块边界 -> 职责分配 -> 交互约束。
- 执行链：TOBE 决策 -> AICoding 任务 -> 最小验证集。
- 风险链：触发风险 -> 缓解设计 -> 验证/回退。

## 成果物协作规则

- `xxx模块详细设计说明书` 只是占位名；同一轮 SDD 工作流中必须解析为同一个目标成果物。
- 本 skill 的写入边界是门禁章节、门禁问题清单、整改清单、复检记录和阻塞说明。
- 本 skill 可以读取 ASIS、TOBE、任务、测试矩阵和风险章节，但不得直接覆盖其正文。
- 只有工作流、用户或成果物拆分规则明确要求时，门禁结果才写入独立门禁成果物；否则优先写回目标成果物的门禁章节。
- 门禁引用的 ASIS 证据编号、需求/AR 编号、TOBE 决策编号和任务编号必须来自目标成果物。

## 门禁结论

门禁结论只能是：

- `通过`：四条链均通过，阻断问题为 0，可进入 AICoding。
- `不通过`：存在必须整改的问题，需回到 ASIS、TOBE 或上游设计对齐。
- `阻塞`：缺少必要输入、代码、上游决策，或连续返工仍因同一外部缺口无法推进。

演练门禁必须分别输出 `skill 有效性结论` 和 `真实 AICoding 准入结论`，不得把演练通过等同于真实准入。

## 工作流程

### 1. 定位目标成果物

- 读取当前 SDD 工作流指定的目标成果物；未指定时使用同一轮流程内统一的 `xxx模块详细设计说明书` 占位。
- 读取 ASIS、TOBE、需求/AR 映射、证据索引、AICoding 任务、测试矩阵、待确认问题和阻塞项。
- 识别运行模式：真实 / 演练。

### 2. 检查证据链

判断需求/AR 是否能追溯到 ASIS 证据和 TOBE 决策：

- 关键 ASIS 结论是否有证据编号。
- 证据编号是否能在证据索引中反查。
- ASIS 结论状态是否保留为事实/推断/待确认/阻塞相关。
- 推断、待确认、低置信度或阻塞相关结论是否被 TOBE 错当成事实。
- TOBE 关键决策是否引用 ASIS 证据。

必须进行 ASIS 倾向采样。至少抽查 3 个 ASIS 结论；少于 3 个则全查。高风险、大规模、跨模块、低置信度、`inferred/mixed` 边界、规格漂移或核心链路影响时，必须风险加抽。

### 3. 检查边界链

判断模块边界和职责是否约束住设计：

- 模块边界是否来自 `softWare.md` 或明确推断。
- 本模块承担和不承担的内容是否清楚。
- TOBE 是否越界承担其他模块职责。
- 跨模块交互是否符合模块影响性分析和模块间交互设计。
- 是否引入循环依赖、跨层调用、外部模型穿透、共享状态扩散、旁路写入或隐式耦合。

存在跨边界改动时，必须检查防腐层、DTO/模型翻译、依赖方向和禁止穿透内容。

### 4. 检查执行链

判断 TOBE 是否能交给 AICoding 执行：

- 每个关键 TOBE 决策是否落到工程位置。
- AICoding 任务是否粒度合适，能直接编码。
- 任务是否有主要改动区域、依赖、验收标准和最小验证集。
- 测试矩阵是否能从需求/AR 追踪到 ASIS 证据、TOBE 决策、任务、测试项和断言方式。
- 是否存在无法阶段性验证的关键设计。

任务过大、改动位置不清、测试不可执行时，执行链不通过。

### 5. 检查风险链

只检查被触发的风险专题，不要求所有专项表都填满：

- 架构防腐化。
- 安全增强。
- 性能增强。
- 日志可观测性。
- 兼容、迁移与回滚。
- 并发、事务与幂等。

每个触发风险都必须有缓解设计、验证方式和必要回退策略。未触发专题应有简短不适用说明。

### 5.1 检查前置问询

- 影响模块边界、核心方案、数据/权限/兼容策略、性能目标、验收阈值或任务拆分的待确认项，是否已在 ASIS 或 TOBE 阶段触发问询。
- 如果没有问询条件，成果物是否标记为 `需前置确认` 并阻止相关决策定稿。
- 如果待确认项可能推翻当前设计，不得给出真实 AICoding 准入通过结论。

### 6. TOBE 严格探索

门禁要主动探索 TOBE 正确性，不只读表格：

- 架构反证：是否可构造出边界腐化或依赖方向错误的反例。
- 替代方案对比：是否存在更小、更贴合现有架构的方案。
- 主流程追踪：入口到副作用是否走通。
- 失败路径追踪：至少覆盖输入异常、外部依赖失败、部分成功或回退场景中的适用项。
- 可测试性追踪：是否能按任务分阶段验证。

探索发现的问题必须进入阻断问题或非阻断改进项。

### 7. 输出门禁结果

加载 `references/gate-result-template.md`，输出四链门禁结果、ASIS 采样、TOBE 探索、阻断问题、整改清单和复检记录。

阻断问题只分四类：

- `EVIDENCE`：证据不足或不确定性被伪装成事实。
- `BOUNDARY`：边界、职责、交互或防腐化不成立。
- `EXECUTION`：任务不可执行、不可验证或粒度失控。
- `RISK`：触发风险没有缓解、验证或回退。

## 多轮复检规则

门禁不通过时：

1. 生成整改清单。
2. 判断每个整改项应回到 ASIS、TOBE、上游设计对齐还是模块间交互设计。
3. 如果当前 agent 能补齐，回到对应阶段更新目标成果物后重新门禁。
4. 如果不能补齐，标记阻塞并说明需要的输入。
5. 复检时保留轮次记录，说明修复了哪些问题，剩余哪些问题。

## 质量标准

结束前确认：

- 门禁结论是 `通过`、`不通过` 或 `阻塞` 之一。
- 四条链均有明确结论。
- 阻断问题与非阻断改进项分离。
- 每个阻断问题都有明确返工阶段和整改要求。
- 通过时明确说明可进入 AICoding。
- 不通过或阻塞时明确说明不可进入 AICoding，或仅允许哪些有限范围进入 AICoding。

## 引用文件

- 核心方法论：`C:\Users\15802\.codex\skills\sdd-module-design-method.md`
- 门禁结果模板：`references/gate-result-template.md`
- 可选检查清单：`references/gate-checklist.md`
